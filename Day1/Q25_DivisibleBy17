import java.util.*;

class DivisibleBy17{
    public static void main(String[] args) {

        List<Integer> list = List.of(10,14,23,35,41);

        boolean result =
        list.stream()
            .anyMatch(n -> n % 7 == 0);

        System.out.println(result);
    }
}
