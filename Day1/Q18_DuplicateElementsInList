import java.util.stream.*;
import java.util.*;
class DuplicateElementsInList{
    public static void main(String[] args) {
        
       List<Integer> list=List.of(1,2,1,2,4,5,5,3,4);
       
       Set<Integer> seen=new HashSet<>();
       
       List<Integer> result=
       list.stream()
       .filter(n->!seen.add(n))
       .distinct()
       .toList();
       System.out.println(result);
    }
}
